{
  "name": "@upscalerjs/core",
  "description": "Core library for UpscalerJS, dependency for upscaler and model packages. Not intended to be used directly.",
  "version": "1.0.0-beta.15",
  "main": "./dist/cjs/index.js",
  "types": "./dist/cjs/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/cjs/index.js",
      "import": "./dist/esm/index.js"
    }
  },
  "files": [
    "LICENSE",
    "dist/**/*"
  ],
  "wireit": {
    "test": {
      "command": "pnpm vitest -c vitest.config.ts"
    },
    "lint": {
      "command": "eslint -c .eslintrc.js src --ext .ts"
    },
    "prepublishOnly": {
      "command": "pnpm lint && pnpm test && pnpm build && pnpm validate"
    },
    "validate": {
      "command": "ts-node ../../scripts/package-scripts/validate-build.ts packages/core -c './dist/esm/index.d.ts' -c './dist/cjs/index.d.ts' "
    },
    "clean:esm": {
      "command": "rimraf dist/esm"
    },
    "clean:cjs": {
      "command": "rimraf dist/cjs"
    },
    "build": {
      "command": "pnpm build:esm && pnpm build:cjs && pnpm validate"
    },
    "build:esm": {
      "command": "tsc -p tsconfig.esm.json --outDir ./dist/esm",
      "dependencies": ["clean:esm"],
      "files": [
        "src/**/*.ts",
        "!src/**/*.test.ts",
        "package.json",
        "vitest.config.ts",
        "tsconfig.esm.json"
      ],
      "output": [
        "dist/esm/**"
      ]
    },
    "build:cjs": {
      "command": "tsc -p tsconfig.cjs.json --outDir ./dist/cjs",
      "dependencies": ["clean:cjs"],
      "files": [
        "src/**/*.ts",
        "!src/**/*.test.ts",
        "package.json",
        "vitest.config.ts",
        "tsconfig.cjs.json"
      ],
      "output": [
        "dist/cjs/**"
      ]
    }
  },
  "scripts": {
    "test": "wireit",
    "lint": "wireit",
    "prepublishOnly": "wireit",
    "validate": "wireit",
    "build": "wireit",
    "build:esm": "wireit",
    "build:cjs": "wireit"
  },
  "dependencies": {
    "@tensorflow/tfjs-core": "~4.8.0"
  },
  "devDependencies": {
    "vitest": "^0.34.2",
    "wireit": "^0.14.0"
  }
}
