{
  "name": "upscaler",
  "version": "0.8.27",
  "description": "Increase Image resolution with Tensorflow.js",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "keywords": [
    "super resolution",
    "image",
    "scale",
    "resize",
    "esrgan",
    "upscaling",
    "upscaler",
    "image enhancement",
    "image super resolution",
    "tensorflow.js",
    "tfjs"
  ],
  "files": [
    "LICENSE",
    "dist/**/*",
    "models/**/*"
  ],
  "scripts": {
    "docs:serve": "docsify serve docs --port 3030",
    "prepare": "yarn build",
    "preversion": "yarn lint",
    "prepublishOnly": "yarn test && yarn lint",
    "format": "echo 'Formatting code' && prettier --write \"src/**/*.ts\"",
    "lint": "echo 'Linting code' && tslint -p tsconfig.json",
    "build": "rimraf dist && yarn build:cjs && yarn build:umd && yarn build:esm",
    "build:cjs": "tsc --module commonjs --target es5 --outDir dist/cjs",
    "build:umd:tsc": "tsc --moduleResolution node --module es2015 --target es5 --outDir dist/tmp",
    "build:umd:rollup": "npx rollup -c rollup.config.js --failAfterWarnings",
    "build:umd:generate": "yarn build:umd:tsc && yarn build:umd:rollup",
    "build:umd:compress": "uglifyjs --source-map --comments --output dist/umd/upscaler.min.js -- dist/umd/upscaler.js",
    "build:umd": "yarn build:umd:generate && yarn build:umd:compress && rimraf dist/umd/tmp",
    "build:esm": "tsc --module esnext --moduleResolution node --target esnext --outDir dist/esm",
    "test": "jest --config jestconfig.json",
    "watch:esm": "watch 'yarn build:esm' ./src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thekevinscott/UpscalerJS.git"
  },
  "author": "Kevin Scott",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/thekevinscott/UpscalerJS/issues"
  },
  "homepage": "https://github.com/thekevinscott/UpscalerJS#readme",
  "peerDependencies": {
    "@tensorflow/tfjs": "^3.13.0"
  },
  "dependencies": {
    "tensor-as-base64": "^0.1.1"
  },
  "gitHead": "cccae63a0a7aeccaa78d30e55a8e12ec72f5c19f"
}