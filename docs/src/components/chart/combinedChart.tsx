import React from 'react';
// import Chart from './chart';

// const translateData = (performanceData: PerformanceResult[], speedData: SpeedResult[]) => {
//   return {
//     labels: [],
//     data: [],
//   };
// }

export default function CombinedChart() {
// export default function CombinedChart({ speedDatabase, performanceDatabase }: { speedDatabase: string; performanceDatabase: string }) {
  // const performanceData = usePerformanceQuery(performanceDatabase);
  // const speedData = useSpeedQuery(speedDatabase);

  // const translatedData = useMemo(() => translateData(performanceData, speedData), [performanceData, speedData]);

  // return (
  //   <Chart type="line" title="Combined Benchmarks" data={translatedData} />
  // );

  return (
    <div>Coming Soon</div>
  )
}
